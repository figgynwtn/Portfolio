'use client'
import { motion } from 'framer-motion'

export default function Footer() {
  return (
    <footer className="relative bg-neon-secondary w-full h-[8rem] flex items-end justify-center overflow-hidden">
      <div className="relative w-full h-[8rem] flex items-end justify-center">
        {/* Placeholder for future footer content */}
        <div className="w-full text-center text-neon-primary font-clash text-xl opacity-50 py-8">
          {/* Footer placeholder */}
          Footer content goes here
        </div>
      </div>
    </footer>
  )
}